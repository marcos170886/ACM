import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:acmsys/app/modules/services/services_module.dart';

void main() {
  initModule(ServicesModule());
  // ServicesController services;
  //
  setUp(() {
    //     services = ServicesModule.to.get<ServicesController>();
  });

  group('ServicesController Test', () {
    //   test("First Test", () {
    //     expect(services, isInstanceOf<ServicesController>());
    //   });

    //   test("Set Value", () {
    //     expect(services.value, equals(0));
    //     services.increment();
    //     expect(services.value, equals(1));
    //   });
  });
}
