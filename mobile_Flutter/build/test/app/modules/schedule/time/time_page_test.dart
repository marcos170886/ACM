import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('TimePage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TimePage(title: 'Time')));
    //  final titleFinder = find.text('Time');
    //  expect(titleFinder, findsOneWidget);
  });
}
