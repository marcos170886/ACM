import 'package:flutter_test/flutter_test.dart';


main() {
  testWidgets('AppointmentsPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AppointmentsPage(title: 'Appointments')));
    //  final titleFinder = find.text('Appointments');
    //  expect(titleFinder, findsOneWidget);
  });
}
