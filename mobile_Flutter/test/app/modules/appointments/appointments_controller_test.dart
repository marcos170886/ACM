import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:acmsys/app/modules/appointments/appointments_module.dart';

void main() {
  initModule(AppointmentsModule());
  // AppointmentsController appointments;
  //
  setUp(() {
    //     appointments = AppointmentsModule.to.get<AppointmentsController>();
  });

  group('AppointmentsController Test', () {
    //   test("First Test", () {
    //     expect(appointments, isInstanceOf<AppointmentsController>());
    //   });

    //   test("Set Value", () {
    //     expect(appointments.value, equals(0));
    //     appointments.increment();
    //     expect(appointments.value, equals(1));
    //   });
  });
}
