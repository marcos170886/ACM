import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:acmsys/app/modules/schedule/schedule_module.dart';

void main() {
  initModule(ScheduleModule());
  // TimeController time;
  //
  setUp(() {
    //     time = ScheduleModule.to.get<TimeController>();
  });

  group('TimeController Test', () {
    //   test("First Test", () {
    //     expect(time, isInstanceOf<TimeController>());
    //   });

    //   test("Set Value", () {
    //     expect(time.value, equals(0));
    //     time.increment();
    //     expect(time.value, equals(1));
    //   });
  });
}
